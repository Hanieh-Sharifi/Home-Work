import React from 'react'

function Enter({resultText}) {
    return (
        <div style={{color: "white", fontSize: "1.5rem", textAlign: "center"}}>
            {resultText}
        </div>
    );
};

export default Enter;
